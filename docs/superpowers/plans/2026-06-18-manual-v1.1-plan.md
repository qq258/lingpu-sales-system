# 使用手册 v1.1 增量更新 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `docs/使用手册.md` 与 `portal/src/views/Manual.vue` 从 v1.0 升级到 v1.1,新增第十章「售后记录」+ 8 个新小节,两版内容逐字一致。

**Architecture:** 在保留 v1.0 现有 9 章 100% 内容不变的基础上,按 spec 规定的位置追加新小节与新章。Markdown 文档版与 Vue 网页版同步实施,文案逐字对齐。复用现有 CSS 类(`.sec`、`.tip`、`.warn`、`.code-block`、`.qa-item`、`.table-wrap`、`.role-tag`),不引入新样式。

**Tech Stack:** Markdown、Vue 3 (Composition API + `<script setup lang="ts">`)、Element Plus、TypeScript

**Spec:** `docs/superpowers/specs/2026-06-18-manual-v1.1-design.md`(本计划的源真理)

---

## 文件结构

| 文件 | 状态 | 职责 |
|------|------|------|
| `docs/使用手册.md` | 修改 | Markdown 文档版手册(主体内容) |
| `portal/src/views/Manual.vue` | 修改 | Vue 网页版手册(同步镜像) |

**单一职责原则**:
- 文档版:以 Markdown 形式表达,支持打印、导出
- 网页版:以 Vue 模板形式表达,支持目录侧边栏与章节切换
- 两版**内容逐字一致**,仅渲染语法不同

**变更边界**:
- 不修改 `client/` 任何文件
- 不修改后端任何文件
- 不修改 `2026-06-17-manual-design.md`
- 不修改 `design-spec.md` 风格规范
- 不引入新依赖、新路由、新组件、新样式类

---

## Task 1: 更新顶部元数据与目录

**Files:**
- Modify: `docs/使用手册.md:1-22`
- Modify: `portal/src/views/Manual.vue:1-5`(徽章) + `portal/src/views/Manual.vue`(chapters 数组)

- [ ] **Step 1: 修改 Markdown 文档版顶部版本号与日期**

将 `docs/使用手册.md` 第 1-3 行:

```markdown
# 手机销售管理系统 — 使用手册

> 版本：1.0 | 更新日期：2026-06-17
```

改为:

```markdown
# 手机销售管理系统 — 使用手册

> 版本：1.1 | 更新日期：2026-06-18
```

- [ ] **Step 2: 修改 Markdown 目录增加第十章锚点**

将 `docs/使用手册.md` 目录区(第 5-15 行附近)末尾,在「9. 常见问题与故障排除」之后追加:

```markdown
10. [售后记录](#十售后记录)
```

- [ ] **Step 3: 修改 Vue 网页版徽章**

将 `portal/src/views/Manual.vue` 第 5 行附近:

```vue
<span class="badge">v1.0</span>
```

改为:

```vue
<span class="badge">v1.1</span>
```

- [ ] **Step 4: 修改 Vue `chapters` 数组新增第 10 项**

将 `portal/src/views/Manual.vue` 中 `chapters` 数组末尾(在 `{ id: 9, label: '常见问题' }` 之后)追加:

```ts
  { id: 10, label: '售后记录' },
```

完整数组应变为:

```ts
const chapters = [
  { id: 1, label: '系统入门' },
  { id: 2, label: '基础数据管理' },
  { id: 3, label: '采购入库' },
  { id: 4, label: '库存管理' },
  { id: 5, label: '销售开单' },
  { id: 6, label: '门店调货' },
  { id: 7, label: '数据统计与看板' },
  { id: 8, label: '系统管理' },
  { id: 9, label: '常见问题' },
  { id: 10, label: '售后记录' },
]
```

- [ ] **Step 5: 验证文件内容**

Run: `grep -n "1.1" docs/使用手册.md` (在 PowerShell 中用 `Select-String`)
Expected: 命中 1 行(版本号 1.1)

Run: `grep -n "v1.1" portal/src/views/Manual.vue` (在 PowerShell 中用 `Select-String "v1.1"`)
Expected: 命中 1 行(徽章 v1.1)

Run: `grep -n "售后记录" portal/src/views/Manual.vue` (在 PowerShell 中用 `Select-String "售后记录"`)
Expected: 命中 1 行(chapters 数组中)

- [ ] **Step 6: 暂不提交(集中到最后统一 commit)**

> 本任务为元数据更新,留到所有任务完成后一次性提交。

---

## Task 2: 追加第三章 3.6 无库存销售-入库场景

**Files:**
- Modify: `docs/使用手册.md`(在 3.5 IMEI2 和 SN 码录入说明 之后)
- Modify: `portal/src/views/Manual.vue`(第三章 `<!-- 第三章 -->` 块内 3.5 之后)

- [ ] **Step 1: 在 Markdown 文档版 3.5 之后插入 3.6**

定位 `docs/使用手册.md` 中 `## 三、采购入库` 章节,找到 `### 3.5 IMEI2 和 SN 码录入说明` 小节(在第三章末尾),在其后追加以下 Markdown:

```markdown

### 3.6 无库存销售-入库场景(门户网站)

> 适用:历史商品在售出时尚未登记入库,需要现录现卖。

1. 登录门户网站,点击顶部导航的**开单**,或从看板点击**开单**快捷按钮
2. **切换销售类型**:在页面顶部"销售类型"区域,选择 **无库存销售**
3. **搜索品牌型号**:在品牌型号输入框中输入关键词,系统从已有型号中模糊匹配(匹配品牌名 + 型号名)
4. **选择商品**:
   - 匹配到已有型号 → 点击下拉项(自动带入预设售价)
   - 无匹配项 → 点击下拉列表底部"未找到匹配项,将保存为新品牌型号",在弹出的确认框中按"保存"
5. **录入 IMEI**:
   - 依次在 IMEI1 / IMEI2 / SN 三个输入框中扫码或手动输入
   - 扫码枪录入时,每字段扫描后自动跳焦到下一字段
6. **设置售出价格**:在"售出价格"输入框中填写(匹配到已有型号时自动带入,允许修改)
7. 点击**添加至购物车**
8. **结算开单**:与普通销售一致,填写实收金额、客户信息(可选),点击**确认开单**
9. 收款完成后,系统自动调后端接口新建品牌型号 + 库存记录

> 注意:无库存销售产生的库存记录会立即出现在库存查询中,后续可正常补录 IMEI2/SN(见 4.7)。
```

- [ ] **Step 2: 在 Vue 网页版第三章 3.5 之后插入 3.6**

定位 `portal/src/views/Manual.vue` 中 `<!-- 第三章 -->` 块,找到 `<!-- 第四章 -->` 之前的最后一个 `<section class="sec">`(3.5 IMEI2 和 SN 码录入)。在该 `</section>` 闭合之后、第三章 `</div>` 闭合之前,插入:

```vue

          <section class="sec">
            <h3 class="sec-title">3.6 无库存销售-入库场景(门户网站)</h3>
            <p><strong>适用</strong>:历史商品在售出时尚未登记入库,需要现录现卖。</p>
            <ol>
              <li>登录门户网站,点击顶部导航的<strong>开单</strong>,或从看板点击<strong>开单</strong>快捷按钮</li>
              <li><strong>切换销售类型</strong>:在页面顶部"销售类型"区域,选择 <strong>无库存销售</strong></li>
              <li><strong>搜索品牌型号</strong>:在品牌型号输入框中输入关键词,系统从已有型号中模糊匹配(匹配品牌名 + 型号名)</li>
              <li><strong>选择商品</strong>:
                <ul>
                  <li>匹配到已有型号 → 点击下拉项(自动带入预设售价)</li>
                  <li>无匹配项 → 点击下拉列表底部"未找到匹配项,将保存为新品牌型号",在弹出的确认框中按"保存"</li>
                </ul>
              </li>
              <li><strong>录入 IMEI</strong>:依次在 IMEI1 / IMEI2 / SN 三个输入框中扫码或手动输入,扫码枪录入时自动跳焦到下一字段</li>
              <li><strong>设置售出价格</strong>:匹配到已有型号时自动带入,允许修改</li>
              <li>点击<strong>添加至购物车</strong></li>
              <li><strong>结算开单</strong>:与普通销售一致,填写实收金额、客户信息(可选),点击<strong>确认开单</strong></li>
              <li>收款完成后,系统自动调后端接口新建品牌型号 + 库存记录</li>
            </ol>
            <div class="tip">无库存销售产生的库存记录会立即出现在库存查询中,后续可正常补录 IMEI2/SN(见 4.7)。</div>
          </section>
```

- [ ] **Step 3: 验证 3.6 在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "3.6 无库存销售"`
Expected: 命中 1 行(标题行)

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "3.6 无库存销售"`
Expected: 命中 1 行

---

## Task 3: 追加第四章 4.7 IMEI2/SN 搜索与补录

**Files:**
- Modify: `docs/使用手册.md`(在 4.6 低库存预警 之后)
- Modify: `portal/src/views/Manual.vue`(第四章 4.6 之后)

- [ ] **Step 1: 在 Markdown 文档版 4.6 之后插入 4.7**

定位 `docs/使用手册.md` 中 `### 4.6 低库存预警` 小节,在其 `</section>` 闭合(在 Markdown 中即下一行 `## 五、销售开单` 之前)后追加:

```markdown

### 4.7 库存查询支持 IMEI2 / SN 码搜索与补录

#### 4.7.1 搜索范围扩展

- 搜索框支持输入:IMEI / IMEI2 / SN 码 / 商品名称 / 厂家条码
- 搜索框 placeholder 改为:`搜索 IMEI / IMEI2 / SN码 / 商品名称...`

#### 4.7.2 卡片展开与补录

1. 库存卡片默认展示品牌、型号、规格、IMEI
2. 点击卡片上的**展开按钮**,显示 IMEI2 和 SN 码
3. 若某字段为空,显示**补录**按钮
4. 点补录 → 行内变为输入框 → 输入后回车 → 调用后端 `PUT /inventory/imei/:id` 保存
5. 保存后卡片自动刷新,显示新值

#### 4.7.3 补录数据流

```
用户点击补录 → 内联输入 → PUT /inventory/imei/:id { imei2?: string, sn_code?: string }
  → 后端更新 wh_inventory_imei 表
  → 返回更新后记录 → 卡片刷新
```
```

> 注意:如果用 Markdown 三反引号时需要转义,使用 `~~~` 围栏代替。

- [ ] **Step 2: 在 Vue 网页版第四章 4.6 之后插入 4.7**

定位 `portal/src/views/Manual.vue` 中第四章 4.6 低库存预警的 `<section class="sec">` 闭合之后,插入:

```vue

          <section class="sec">
            <h3 class="sec-title">4.7 库存查询支持 IMEI2 / SN 码搜索与补录</h3>

            <section class="sub-sec">
              <h4 class="sub-title">4.7.1 搜索范围扩展</h4>
              <ul>
                <li>搜索框支持输入:IMEI / IMEI2 / SN 码 / 商品名称 / 厂家条码</li>
                <li>搜索框 placeholder 改为:<code>搜索 IMEI / IMEI2 / SN码 / 商品名称...</code></li>
              </ul>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">4.7.2 卡片展开与补录</h4>
              <ol>
                <li>库存卡片默认展示品牌、型号、规格、IMEI</li>
                <li>点击卡片上的<strong>展开按钮</strong>,显示 IMEI2 和 SN 码</li>
                <li>若某字段为空,显示<strong>补录</strong>按钮</li>
                <li>点补录 → 行内变为输入框 → 输入后回车 → 调用后端 <code>PUT /inventory/imei/:id</code> 保存</li>
                <li>保存后卡片自动刷新,显示新值</li>
              </ol>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">4.7.3 补录数据流</h4>
              <div class="code-block">用户点击补录 → 内联输入 → PUT /inventory/imei/:id { imei2?: string, sn_code?: string }
  → 后端更新 wh_inventory_imei 表
  → 返回更新后记录 → 卡片刷新</div>
            </section>
          </section>
```

- [ ] **Step 3: 验证 4.7 在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "4.7 库存查询支持"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "4.7 库存查询支持"`
Expected: 命中 1 行

---

## Task 4: 追加第五章 5.7 无库存销售(开单场景)

**Files:**
- Modify: `docs/使用手册.md`(在 5.6 销售记录查询 之后)
- Modify: `portal/src/views/Manual.vue`(第五章 5.6 之后)

- [ ] **Step 1: 在 Markdown 文档版 5.6 之后插入 5.7**

定位 `docs/使用手册.md` 中 `### 5.6 销售记录查询` 小节,在其后追加:

```markdown

### 5.7 无库存销售(开单场景)

> 提示:与 3.6 互补 — 3.6 描述入库逻辑,5.7 描述开单完整流程。

#### 5.7.1 切换销售类型

- 在门户网站开单页顶部,新增 radio 切换:`普通销售` / `无库存销售`
- 默认 `普通销售`;切换时若购物车有商品,弹窗确认后清空

#### 5.7.2 无库存销售模式

- **品牌型号搜索**:支持模糊搜索,无匹配时允许用户输入并保存为新品牌型号(弹 `SaveBrandDialog` 二次确认)
- **保存品牌型号**:弹窗中按第一个空格自动分割为品牌 + 型号,用户可调整;只允许点"确认"(无取消)
- **IMEI/SN 录入**:扫码枪逐字段自动跳焦
- **购物车**:每个商品展示 IMEI1/IMEI2/SN,未录入字段显示`[待录入]`标签,点击行内编辑
- **结算**:与普通销售一致(合计、实收、找零、客户信息、备注)

#### 5.7.3 提交后自动建库

- 收款确认后,后端 `POST /sales/no-stock` 一次性完成:新建品牌型号 + 新建库存记录 + 创建销售单 + 扣减库存
```

- [ ] **Step 2: 在 Vue 网页版第五章 5.6 之后插入 5.7**

定位 `portal/src/views/Manual.vue` 中第五章 5.6 销售记录查询的 `<section class="sec">` 闭合之后,插入:

```vue

          <section class="sec">
            <h3 class="sec-title">5.7 无库存销售(开单场景)</h3>
            <div class="tip">与 3.6 互补 — 3.6 描述入库逻辑,5.7 描述开单完整流程。</div>

            <section class="sub-sec">
              <h4 class="sub-title">5.7.1 切换销售类型</h4>
              <ul>
                <li>在门户网站开单页顶部,新增 radio 切换:<code>普通销售</code> / <code>无库存销售</code></li>
                <li>默认 <code>普通销售</code>;切换时若购物车有商品,弹窗确认后清空</li>
              </ul>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">5.7.2 无库存销售模式</h4>
              <ul>
                <li><strong>品牌型号搜索</strong>:支持模糊搜索,无匹配时允许用户输入并保存为新品牌型号(弹 SaveBrandDialog 二次确认)</li>
                <li><strong>保存品牌型号</strong>:弹窗中按第一个空格自动分割为品牌 + 型号,用户可调整;只允许点"确认"(无取消)</li>
                <li><strong>IMEI/SN 录入</strong>:扫码枪逐字段自动跳焦</li>
                <li><strong>购物车</strong>:每个商品展示 IMEI1/IMEI2/SN,未录入字段显示[待录入]标签,点击行内编辑</li>
                <li><strong>结算</strong>:与普通销售一致(合计、实收、找零、客户信息、备注)</li>
              </ul>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">5.7.3 提交后自动建库</h4>
              <p>收款确认后,后端 <code>POST /sales/no-stock</code> 一次性完成:新建品牌型号 + 新建库存记录 + 创建销售单 + 扣减库存</p>
            </section>
          </section>
```

- [ ] **Step 3: 验证 5.7 在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "5.7 无库存销售"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "5.7 无库存销售"`
Expected: 命中 1 行

---

## Task 5: 追加第七章 7.5 + 7.6

**Files:**
- Modify: `docs/使用手册.md`(在 7.4 热销排行 之后)
- Modify: `portal/src/views/Manual.vue`(第七章 7.4 之后)

- [ ] **Step 1: 在 Markdown 文档版 7.4 之后插入 7.5**

定位 `docs/使用手册.md` 中 `### 7.4 热销排行` 小节,在其后追加:

```markdown

### 7.5 Dashboard 核心指标卡(StatCard)

新版 portal 看板顶部新增三个 StatCard:

| 卡片 | 数据来源 | 说明 |
|------|---------|------|
| 今日销售额 | `/stats/dashboard.todaySales` | 当天所有已售订单总金额(¥ 前缀) |
| 今日订单数 | `/stats/dashboard.todayOrders` | 当天完成的销售订单数 |
| 低库存预警数 | `/stats/dashboard.lowStockCount` | 库存低于 10 台的型号数 |

#### 7.5.1 快速开单区简化

- 移除原 Dashboard 内嵌的完整销售表单(品牌搜索/购物车/结算)
- 改为**快速扫码入口**:
  - 单行 IMEI 输入框(扫码枪或手动输入)
  - 点**去开单** → 跳转到 `/sale?imei=xxx` 自动带入 IMEI
  - 提供文字链接**前往完整开单页**直达 `/sale`

### 7.6 IMEI 查询弹窗(完整追踪链路)

入口:portal 看板快捷按钮 **IMEI查询**,或顶部导航同名 Tab。

#### 7.6.1 查询触发

- 输入完整 IMEI,回车查询
- 后端调用 `GET /api/v1/inventory/imei-query?imei=xxx`

#### 7.6.2 展示字段

**基础信息**(始终展示):
品牌、型号、颜色、存储(RAM/ROM)、IMEI、IMEI2、SN 码、状态(在库/已售)、所在门店、售价

**入库记录**(始终展示):
入库单号、供应商名称、入库时间

> ⚠️ 入库单价不展示(成本隐私)

**销售记录**(仅已售状态展示):
订单号、售出门店、售出时间、应收金额、实收金额、找零、客户姓名
客户电话(默认脱敏,可点显示)
客户地址(默认脱敏,可点显示)
收银员

#### 7.6.3 状态对应场景

- `in_stock` → 展示基础 + 入库记录
- `sold` → 展示基础 + 入库记录 + 销售记录
```

- [ ] **Step 2: 在 Vue 网页版第七章 7.4 之后插入 7.5 + 7.6**

定位 `portal/src/views/Manual.vue` 中第七章 7.4 热销排行的 `<section class="sec">` 闭合之后,插入:

```vue

          <section class="sec">
            <h3 class="sec-title">7.5 Dashboard 核心指标卡(StatCard)</h3>
            <p>新版 portal 看板顶部新增三个 StatCard:</p>
            <div class="table-wrap">
              <table>
                <thead><tr><th>卡片</th><th>数据来源</th><th>说明</th></tr></thead>
                <tbody>
                  <tr><td>今日销售额</td><td><code>/stats/dashboard.todaySales</code></td><td>当天所有已售订单总金额(¥ 前缀)</td></tr>
                  <tr><td>今日订单数</td><td><code>/stats/dashboard.todayOrders</code></td><td>当天完成的销售订单数</td></tr>
                  <tr><td>低库存预警数</td><td><code>/stats/dashboard.lowStockCount</code></td><td>库存低于 10 台的型号数</td></tr>
                </tbody>
              </table>
            </div>

            <section class="sub-sec">
              <h4 class="sub-title">7.5.1 快速开单区简化</h4>
              <ul>
                <li>移除原 Dashboard 内嵌的完整销售表单(品牌搜索/购物车/结算)</li>
                <li>改为<strong>快速扫码入口</strong>:
                  <ul>
                    <li>单行 IMEI 输入框(扫码枪或手动输入)</li>
                    <li>点<strong>去开单</strong> → 跳转到 <code>/sale?imei=xxx</code> 自动带入 IMEI</li>
                    <li>提供文字链接<strong>前往完整开单页</strong>直达 <code>/sale</code></li>
                  </ul>
                </li>
              </ul>
            </section>
          </section>

          <section class="sec">
            <h3 class="sec-title">7.6 IMEI 查询弹窗(完整追踪链路)</h3>
            <p>入口:portal 看板快捷按钮 <strong>IMEI查询</strong>,或顶部导航同名 Tab。</p>

            <section class="sub-sec">
              <h4 class="sub-title">7.6.1 查询触发</h4>
              <ul>
                <li>输入完整 IMEI,回车查询</li>
                <li>后端调用 <code>GET /api/v1/inventory/imei-query?imei=xxx</code></li>
              </ul>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">7.6.2 展示字段</h4>
              <p><strong>基础信息</strong>(始终展示):</p>
              <p>品牌、型号、颜色、存储(RAM/ROM)、IMEI、IMEI2、SN 码、状态(在库/已售)、所在门店、售价</p>
              <p><strong>入库记录</strong>(始终展示):</p>
              <p>入库单号、供应商名称、入库时间</p>
              <div class="warn">入库单价不展示(成本隐私)</div>
              <p><strong>销售记录</strong>(仅已售状态展示):</p>
              <p>订单号、售出门店、售出时间、应收金额、实收金额、找零、客户姓名</p>
              <p>客户电话(默认脱敏,可点显示)</p>
              <p>客户地址(默认脱敏,可点显示)</p>
              <p>收银员</p>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">7.6.3 状态对应场景</h4>
              <ul>
                <li><code>in_stock</code> → 展示基础 + 入库记录</li>
                <li><code>sold</code> → 展示基础 + 入库记录 + 销售记录</li>
              </ul>
            </section>
          </section>
```

- [ ] **Step 3: 验证 7.5 / 7.6 在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "7.5 Dashboard"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "7.5 Dashboard"`
Expected: 命中 1 行

Run: `Select-String -Path "docs/使用手册.md" -Pattern "7.6 IMEI"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "7.6 IMEI"`
Expected: 命中 1 行

---

## Task 6: 扩充第八章 8.3 + 追加 8.4

**Files:**
- Modify: `docs/使用手册.md`(替换 8.3 数据工具,追加 8.4)
- Modify: `portal/src/views/Manual.vue`(第八章 8.3 替换,追加 8.4)

- [ ] **Step 1: 替换 Markdown 文档版 8.3 现有内容**

定位 `docs/使用手册.md` 中 `### 8.3 数据工具` 小节,从标题到下一行 `## 九、常见问题与故障排除` 之前。**完整替换**为:

```markdown

### 8.3 数据工具(扩展)

> 适用场景:数据备份/导入/导出/批量修正,仅**超级管理员**可操作。
> ⚠️ 操作不可逆,执行前请先下载整库备份。

#### 8.3.1 入口

- 管理后台左侧菜单新增**数据工具**项
- 路由:`/tools`,三个 Tab:**数据备份** / **数据导入** / **数据导出**

#### 8.3.2 数据备份

1. 显示当前数据库信息(大小、表数量、记录数)
2. **下载整库备份**:点**下载 .sqlite 备份** → 浏览器下载 `database.sqlite`

#### 8.3.3 数据导入

1. 选择目标表(供应商/品牌/型号/库存/销售记录/入库单/库存流水)
2. 上传 Excel(.xlsx)文件
3. 系统预览前 10 行,按表校验(必填项/唯一性/外键)
4. 确认导入,事务保护下逐行写入
5. 返回结果:成功 N 条 / 失败 M 条 + 错误详情

#### 8.3.4 数据导出

- 表格展示所有可导出表(中文名 + 记录数)
- 点对应表 → 下载 Excel
- 部分模块支持带筛选条件导出(见 8.4)

#### 8.3.5 导入校验规则

| 表 | 必填 | 校验 |
|----|------|------|
| 供应商 | name | 名称非空 |
| 品牌 | name | 名称不重复 |
| 型号 | brand_id, name | 同品牌下不重复 |
| 库存 | model_id, store_id, quantity | 型号/门店必须存在 |
| 销售记录 | order_no | 单号不重复 |

### 8.4 列表页快捷导出(新增)

下列模块的列表页工具栏右侧新增**导出 Excel** 按钮(点击带当前筛选条件导出):

| 模块 | 路由前缀 | 接口 |
|------|---------|------|
| 供应商 | `/purchase/suppliers/export` | 支持 keyword 筛选 |
| 库存 | `/inventory/export` | 支持 storeId/keyword 筛选 |
| 库存流水 | `/inventory/logs/export` | 支持 changeType/日期筛选 |
| 销售记录 | `/sales/sales/export` | 支持日期/门店筛选 |
| 品牌 | `/products/brands/export` | 含型号数量 |
| 型号 | `/products/models/export` | 含品牌名 |
```

- [ ] **Step 2: 替换 Vue 网页版第八章 8.3 现有内容**

定位 `portal/src/views/Manual.vue` 中第八章 8.3 数据工具的 `<section class="sec">`,**完整替换**为:

```vue

          <section class="sec">
            <h3 class="sec-title">8.3 数据工具(扩展)</h3>
            <div class="role-tag">仅超级管理员可操作</div>
            <p><strong>适用场景</strong>:数据备份/导入/导出/批量修正。</p>
            <div class="warn">操作不可逆,执行前请先下载整库备份。</div>

            <section class="sub-sec">
              <h4 class="sub-title">8.3.1 入口</h4>
              <ul>
                <li>管理后台左侧菜单新增<strong>数据工具</strong>项</li>
                <li>路由:<code>/tools</code>,三个 Tab:<strong>数据备份</strong> / <strong>数据导入</strong> / <strong>数据导出</strong></li>
              </ul>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">8.3.2 数据备份</h4>
              <ol>
                <li>显示当前数据库信息(大小、表数量、记录数)</li>
                <li><strong>下载整库备份</strong>:点<strong>下载 .sqlite 备份</strong> → 浏览器下载 <code>database.sqlite</code></li>
              </ol>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">8.3.3 数据导入</h4>
              <ol>
                <li>选择目标表(供应商/品牌/型号/库存/销售记录/入库单/库存流水)</li>
                <li>上传 Excel(.xlsx)文件</li>
                <li>系统预览前 10 行,按表校验(必填项/唯一性/外键)</li>
                <li>确认导入,事务保护下逐行写入</li>
                <li>返回结果:成功 N 条 / 失败 M 条 + 错误详情</li>
              </ol>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">8.3.4 数据导出</h4>
              <ul>
                <li>表格展示所有可导出表(中文名 + 记录数)</li>
                <li>点对应表 → 下载 Excel</li>
                <li>部分模块支持带筛选条件导出(见 8.4)</li>
              </ul>
            </section>

            <section class="sub-sec">
              <h4 class="sub-title">8.3.5 导入校验规则</h4>
              <div class="table-wrap">
                <table>
                  <thead><tr><th>表</th><th>必填</th><th>校验</th></tr></thead>
                  <tbody>
                    <tr><td>供应商</td><td>name</td><td>名称非空</td></tr>
                    <tr><td>品牌</td><td>name</td><td>名称不重复</td></tr>
                    <tr><td>型号</td><td>brand_id, name</td><td>同品牌下不重复</td></tr>
                    <tr><td>库存</td><td>model_id, store_id, quantity</td><td>型号/门店必须存在</td></tr>
                    <tr><td>销售记录</td><td>order_no</td><td>单号不重复</td></tr>
                  </tbody>
                </table>
              </div>
            </section>
          </section>

          <section class="sec">
            <h3 class="sec-title">8.4 列表页快捷导出(新增)</h3>
            <p>下列模块的列表页工具栏右侧新增<strong>导出 Excel</strong> 按钮(点击带当前筛选条件导出):</p>
            <div class="table-wrap">
              <table>
                <thead><tr><th>模块</th><th>路由前缀</th><th>接口</th></tr></thead>
                <tbody>
                  <tr><td>供应商</td><td><code>/purchase/suppliers/export</code></td><td>支持 keyword 筛选</td></tr>
                  <tr><td>库存</td><td><code>/inventory/export</code></td><td>支持 storeId/keyword 筛选</td></tr>
                  <tr><td>库存流水</td><td><code>/inventory/logs/export</code></td><td>支持 changeType/日期筛选</td></tr>
                  <tr><td>销售记录</td><td><code>/sales/sales/export</code></td><td>支持日期/门店筛选</td></tr>
                  <tr><td>品牌</td><td><code>/products/brands/export</code></td><td>含型号数量</td></tr>
                  <tr><td>型号</td><td><code>/products/models/export</code></td><td>含品牌名</td></tr>
                </tbody>
              </table>
            </div>
          </section>
```

- [ ] **Step 3: 验证 8.3 / 8.4 在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "8.3 数据工具"`
Expected: 命中 1 行(标题行)

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "8.3 数据工具"`
Expected: 命中 1 行

Run: `Select-String -Path "docs/使用手册.md" -Pattern "8.4 列表页快捷导出"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "8.4 列表页快捷导出"`
Expected: 命中 1 行

---

## Task 7: 追加第九章 9.7 + 9.8 FAQ

**Files:**
- Modify: `docs/使用手册.md`(在 9.6 数据工具使用场景 之后)
- Modify: `portal/src/views/Manual.vue`(第九章 9.6 之后)

- [ ] **Step 1: 在 Markdown 文档版 9.6 之后插入 9.7 + 9.8**

定位 `docs/使用手册.md` 中 `### 9.6 数据工具使用场景` 小节,在其 `</section>` 闭合(在 Markdown 中即下一行 `## 十、...` 或文档末尾)后追加:

```markdown

### 9.7 售后记录 FAQ

- **Q:工单能删除吗?**
  仅 `待处理` 状态可删。

- **Q:维修中手机能继续销售吗?**
  否,IMEI 状态为 `维修中` 时销售扫码会拦截。

- **Q:换货时选不到新机?**
  检查新机是否在当前门店库存且 `在库` 状态。

- **Q:工单能跨店操作吗?**
  否,工单归属工单创建时的门店。

- **Q:供应商沟通怎么追溯?**
  工单详情"操作日志"区按时间线展示。

### 9.8 IMEI 查询 FAQ

- **Q:查询后看不到销售记录?**
  该手机可能仍在库,只有 `已售` 状态会展示销售记录。

- **Q:客户电话为什么显示星号?**
  默认脱敏保护隐私,点击该行可显示完整。

- **Q:IMEI2/SN 搜索不到?**
  确认 IMEI2/SN 是否已入库,空值不参与搜索。

- **Q:查询弹窗与销售扫码冲突?**
  不冲突,销售扫码用 `scan-imei`(校验已售),查询用 `imei-query`(全状态)。
```

- [ ] **Step 2: 在 Vue 网页版第九章 9.6 之后插入 9.7 + 9.8**

定位 `portal/src/views/Manual.vue` 中第九章 9.6 数据工具使用场景的 `<section class="sec">` 闭合之后,插入:

```vue

          <section class="sec">
            <h3 class="sec-title">9.7 售后记录 FAQ</h3>
            <div class="qa-item">
              <div class="qa-q">Q:工单能删除吗?</div>
              <div class="qa-a">仅 <code>待处理</code> 状态可删。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:维修中手机能继续销售吗?</div>
              <div class="qa-a">否,IMEI 状态为 <code>维修中</code> 时销售扫码会拦截。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:换货时选不到新机?</div>
              <div class="qa-a">检查新机是否在当前门店库存且 <code>在库</code> 状态。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:工单能跨店操作吗?</div>
              <div class="qa-a">否,工单归属工单创建时的门店。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:供应商沟通怎么追溯?</div>
              <div class="qa-a">工单详情"操作日志"区按时间线展示。</div>
            </div>
          </section>

          <section class="sec">
            <h3 class="sec-title">9.8 IMEI 查询 FAQ</h3>
            <div class="qa-item">
              <div class="qa-q">Q:查询后看不到销售记录?</div>
              <div class="qa-a">该手机可能仍在库,只有 <code>已售</code> 状态会展示销售记录。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:客户电话为什么显示星号?</div>
              <div class="qa-a">默认脱敏保护隐私,点击该行可显示完整。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:IMEI2/SN 搜索不到?</div>
              <div class="qa-a">确认 IMEI2/SN 是否已入库,空值不参与搜索。</div>
            </div>
            <div class="qa-item">
              <div class="qa-q">Q:查询弹窗与销售扫码冲突?</div>
              <div class="qa-a">不冲突,销售扫码用 <code>scan-imei</code>(校验已售),查询用 <code>imei-query</code>(全状态)。</div>
            </div>
          </section>
```

- [ ] **Step 3: 验证 9.7 / 9.8 在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "9.7 售后记录 FAQ"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "9.7 售后记录 FAQ"`
Expected: 命中 1 行

Run: `Select-String -Path "docs/使用手册.md" -Pattern "9.8 IMEI 查询 FAQ"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "9.8 IMEI 查询 FAQ"`
Expected: 命中 1 行

---

## Task 8: 新增第十章 售后记录

**Files:**
- Modify: `docs/使用手册.md`(在文档末尾 `*本文档由系统自动生成...*` 之前追加第十章)
- Modify: `portal/src/views/Manual.vue`(在 `<!-- 第九章 -->` 块 `</div>` 之后,`</div>`(manual-content)之前,插入 `<!-- 第十章 -->` 块)

- [ ] **Step 1: 在 Markdown 文档版追加第十章**

定位 `docs/使用手册.md` 末尾,在 `*本文档由系统自动生成,如有疑问请联系系统管理员。*` 之前追加:

```markdown
## 十、售后记录

> 本章操作在**管理后台(端口 5173)「售后管理」** 完成新建与处理;**门户网站(端口 5174)** 通过顶部「售后」Tab 查看工单与处理进度。

### 10.1 售后场景概述

三种售后类型对照表:

| 场景 | 说明 | 库存影响 |
|------|------|---------|
| 维修(repair) | 当场修 / 店里修 / 返厂修 | 维修期间手机状态置为 `维修中`,完结后恢复 `已售` |
| 换货(exchange) | 从库存换出另一台(可不限制同品牌型号) | 原手机 → `已换货`;新手机 `在库` → `已售` |
| 退款(refund) | 原手机退回,退款给客户 | 原手机 → `已退货` |

### 10.2 售后工单状态流转

两个独立维度:

- **主状态**:`待处理` → `检测中` → (`维修中` → `已维修` / `换货中` → `已换货` / `退款中` → `已退款`) → `已完成`,可任意阶段 `已取消`
- **供应商子状态**:`无需供应商` → `待联系供应商` → `供应商处理中` → `供应商已处理`

> 提示:供应商子状态独立运行,可在主状态的任意阶段触发,完成后回到主流程继续。

### 10.3 新建工单

操作步骤(管理后台):
1. 进入管理后台 > **售后管理** > **新建工单**
2. **扫码/搜索 IMEI**:光标聚焦 IMEI 输入框,扫码或输入后回车,系统自动匹配手机信息(品牌/型号/颜色/存储/原销售记录)
3. 填写**客户信息**:姓名、电话、地址(选填)
4. 填写**故障描述**(必填)
5. 点击**创建工单**,系统自动生成工单号(格式:`ASyyMMddHHmmss` + 3 位随机)

> 提示:未找到该 IMEI 记录则无法创建工单(该手机未在本店售出)。

### 10.4 维修处理

1. 进入工单详情,选择处理方式 → **维修**
2. 选择**维修级别**:`当场` / `店里` / `返厂`
3. 填写费用(可选)和费用说明
4. 提交后手机 IMEI 状态置为 `维修中`
5. 维修完成后,在操作区点**已维修**,手机状态恢复 `已售`

### 10.5 换货处理

1. 进入工单详情,选择处理方式 → **换货**
2. 填写**换货新机**:选择新手机型号 + 录入新 IMEI
3. 提交后原手机状态置为 `已换货`,新手机从库存扣减
4. 完成后点**已换货**,工单进入 `已完成`

> 注意:换货时新手机不限品牌型号,但必须在本店库存中。

### 10.6 退款处理

1. 进入工单详情,选择处理方式 → **退款**
2. 填写退款金额和说明
3. 提交后原手机状态置为 `已退货`
4. 完成后点**已退款**,工单进入 `已完成`
5. 原销售单状态保持不变(已售事实不变)

### 10.7 供应商沟通

适用于返厂维修/退货给供应商等场景:
1. 在工单详情操作区,设置**供应商状态**为`待联系`
2. 在底部日志输入框填写沟通内容,点击**添加日志**(action = `supplier_contact`)
3. 供应商处理中时切换状态为`供应商处理中`
4. 供应商处理完毕,切换为`供应商已处理`,并填写**供应商处理结果**

### 10.8 工单查询与详情

**查询(管理后台):** 售后管理列表,支持按工单号/IMEI/客户姓名/电话/状态/处理方式/日期范围筛选,支持分页。超级管理员可按门店筛选。

**详情(管理后台 / 门户网站通用):**
- 顶部三卡片:手机信息 | 客户信息 | 当前状态(带状态标签)
- 故障描述
- 处理操作区(检测结果/处理方式/费用/供应商信息)
- 操作日志流(时间线展示)

### 10.9 角色与权限

| 角色 | 可执行操作 |
|------|----------|
| 超级管理员 | 全部门店工单查看 + 处理 + 统计 |
| 店长 | 本店工单查看 + 处理 + 新建 |
| 店员 | 本店工单查看 |

> 提示:售后工单号一旦生成不可修改,删除仅限 `待处理` 状态。

> 备注:售后记录是 v1.1 新增模块,操作涉及库存状态变更,请处理前先确认工单对应的手机实物状态。

```

- [ ] **Step 2: 在 Vue 网页版第九章 `</div>` 之后插入第十章**

定位 `portal/src/views/Manual.vue` 中第九章 `<!-- 第九章 -->` 块的 `</div>` 闭合之后,`</div>`(manual-content 结束)之前,插入:

```vue

        <!-- 第十章 -->
        <div v-show="activeChapter === 10" class="chapter glass">
          <h2 class="ch-title">十、售后记录</h2>
          <div class="role-tag">管理后台操作 · 店长或超级管理员权限</div>

          <section class="sec">
            <h3 class="sec-title">10.1 售后场景概述</h3>
            <p>三种售后类型对照表:</p>
            <div class="table-wrap">
              <table>
                <thead><tr><th>场景</th><th>说明</th><th>库存影响</th></tr></thead>
                <tbody>
                  <tr><td>维修(repair)</td><td>当场修 / 店里修 / 返厂修</td><td>维修期间手机状态置为 <code>维修中</code>,完结后恢复 <code>已售</code></td></tr>
                  <tr><td>换货(exchange)</td><td>从库存换出另一台(可不限制同品牌型号)</td><td>原手机 → <code>已换货</code>;新手机 <code>在库</code> → <code>已售</code></td></tr>
                  <tr><td>退款(refund)</td><td>原手机退回,退款给客户</td><td>原手机 → <code>已退货</code></td></tr>
                </tbody>
              </table>
            </div>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.2 售后工单状态流转</h3>
            <p>两个独立维度:</p>
            <ul>
              <li><strong>主状态</strong>:<code>待处理</code> → <code>检测中</code> → (<code>维修中</code> → <code>已维修</code> / <code>换货中</code> → <code>已换货</code> / <code>退款中</code> → <code>已退款</code>) → <code>已完成</code>,可任意阶段 <code>已取消</code></li>
              <li><strong>供应商子状态</strong>:<code>无需供应商</code> → <code>待联系供应商</code> → <code>供应商处理中</code> → <code>供应商已处理</code></li>
            </ul>
            <div class="tip">供应商子状态独立运行,可在主状态的任意阶段触发,完成后回到主流程继续。</div>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.3 新建工单</h3>
            <p><strong>操作步骤(管理后台):</strong></p>
            <ol>
              <li>进入管理后台 &gt; <strong>售后管理</strong> &gt; <strong>新建工单</strong></li>
              <li><strong>扫码/搜索 IMEI</strong>:光标聚焦 IMEI 输入框,扫码或输入后回车,系统自动匹配手机信息(品牌/型号/颜色/存储/原销售记录)</li>
              <li>填写<strong>客户信息</strong>:姓名、电话、地址(选填)</li>
              <li>填写<strong>故障描述</strong>(必填)</li>
              <li>点击<strong>创建工单</strong>,系统自动生成工单号(格式:<code>ASyyMMddHHmmss</code> + 3 位随机)</li>
            </ol>
            <div class="tip">未找到该 IMEI 记录则无法创建工单(该手机未在本店售出)。</div>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.4 维修处理</h3>
            <ol>
              <li>进入工单详情,选择处理方式 → <strong>维修</strong></li>
              <li>选择<strong>维修级别</strong>:<code>当场</code> / <code>店里</code> / <code>返厂</code></li>
              <li>填写费用(可选)和费用说明</li>
              <li>提交后手机 IMEI 状态置为 <code>维修中</code></li>
              <li>维修完成后,在操作区点<strong>已维修</strong>,手机状态恢复 <code>已售</code></li>
            </ol>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.5 换货处理</h3>
            <ol>
              <li>进入工单详情,选择处理方式 → <strong>换货</strong></li>
              <li>填写<strong>换货新机</strong>:选择新手机型号 + 录入新 IMEI</li>
              <li>提交后原手机状态置为 <code>已换货</code>,新手机从库存扣减</li>
              <li>完成后点<strong>已换货</strong>,工单进入 <code>已完成</code></li>
            </ol>
            <div class="warn">换货时新手机不限品牌型号,但必须在本店库存中。</div>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.6 退款处理</h3>
            <ol>
              <li>进入工单详情,选择处理方式 → <strong>退款</strong></li>
              <li>填写退款金额和说明</li>
              <li>提交后原手机状态置为 <code>已退货</code></li>
              <li>完成后点<strong>已退款</strong>,工单进入 <code>已完成</code></li>
              <li>原销售单状态保持不变(已售事实不变)</li>
            </ol>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.7 供应商沟通</h3>
            <p>适用于返厂维修/退货给供应商等场景:</p>
            <ol>
              <li>在工单详情操作区,设置<strong>供应商状态</strong>为<code>待联系</code></li>
              <li>在底部日志输入框填写沟通内容,点击<strong>添加日志</strong>(action = <code>supplier_contact</code>)</li>
              <li>供应商处理中时切换状态为<code>供应商处理中</code></li>
              <li>供应商处理完毕,切换为<code>供应商已处理</code>,并填写<strong>供应商处理结果</strong></li>
            </ol>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.8 工单查询与详情</h3>
            <p><strong>查询(管理后台):</strong> 售后管理列表,支持按工单号/IMEI/客户姓名/电话/状态/处理方式/日期范围筛选,支持分页。超级管理员可按门店筛选。</p>
            <p><strong>详情(管理后台 / 门户网站通用):</strong></p>
            <ul>
              <li>顶部三卡片:手机信息 | 客户信息 | 当前状态(带状态标签)</li>
              <li>故障描述</li>
              <li>处理操作区(检测结果/处理方式/费用/供应商信息)</li>
              <li>操作日志流(时间线展示)</li>
            </ul>
          </section>

          <section class="sec">
            <h3 class="sec-title">10.9 角色与权限</h3>
            <div class="table-wrap">
              <table>
                <thead><tr><th>角色</th><th>可执行操作</th></tr></thead>
                <tbody>
                  <tr><td>超级管理员</td><td>全部门店工单查看 + 处理 + 统计</td></tr>
                  <tr><td>店长</td><td>本店工单查看 + 处理 + 新建</td></tr>
                  <tr><td>店员</td><td>本店工单查看</td></tr>
                </tbody>
              </table>
            </div>
            <div class="tip">售后工单号一旦生成不可修改,删除仅限 <code>待处理</code> 状态。</div>
            <div class="warn">售后记录是 v1.1 新增模块,操作涉及库存状态变更,请处理前先确认工单对应的手机实物状态。</div>
          </section>
        </div>
```

- [ ] **Step 3: 验证第十章在两版中均存在**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "## 十、售后记录"`
Expected: 命中 1 行(标题行)

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "十、售后记录"`
Expected: 命中 1 行

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "activeChapter === 10"`
Expected: 命中 1 行

---

## Task 9: 整体验证

**Files:** (无文件修改,纯验证)

- [ ] **Step 1: 验证 Markdown 文档结构完整**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "^## "` (匹配所有二级标题)
Expected: 命中 10 行(系统入门 / 基础数据管理 / 采购入库 / 库存管理 / 销售开单 / 门店调货 / 数据统计与看板 / 系统管理 / 常见问题与故障排除 / 售后记录)

Run: `Select-String -Path "docs/使用手册.md" -Pattern "^### "` (匹配所有三级标题)
Expected: 命中 ≥ 50 行(原 9 章 + 新增小节)

- [ ] **Step 2: 验证 Vue 网页版章节切换**

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "activeChapter === \d+"`
Expected: 命中 10 行(`=== 1` 到 `=== 10`)

- [ ] **Step 3: 验证 Vue `chapters` 数组**

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "id: \d+"`
Expected: 命中 10 行(1 到 10)

- [ ] **Step 4: 验证 v1.0 现有 9 章保留**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "^# 手机销售管理系统 — 使用手册$"`
Expected: 命中 1 行(主标题未变)

Run: `Select-String -Path "portal/src/views/Manual.vue" -Pattern "1.1 系统入门|2.1 品牌管理|3.1 入库前准备|4.1 库存查询|5.1 门户网站销售开单|6.1 调货流程概述|7.1 门户网站数据看板|8.1 用户管理|9.1 登录问题"`
Expected: 全部命中 9 行(v1.0 现有 9 章首个小节标题未变)

- [ ] **Step 5: 运行 Vue 类型检查**

Run: `cd portal && npx vue-tsc --noEmit`
Expected: 无类型错误(可能首次运行需要 `npm install`)

- [ ] **Step 6: 验证目录锚点**

Run: `Select-String -Path "docs/使用手册.md" -Pattern "10\. \[售后记录\]"`
Expected: 命中 1 行(目录行)

- [ ] **Step 7: 提交(待用户明确要求时执行)**

> 本任务为最终验证。所有修改待用户明确要求 commit 后再执行 git commit。命令样例:

```bash
git add docs/使用手册.md portal/src/views/Manual.vue docs/superpowers/specs/2026-06-18-manual-v1.1-design.md docs/superpowers/plans/2026-06-18-manual-v1.1-plan.md
git commit -m "docs(manual): 升级使用手册到 v1.1,新增第十章售后记录与 8 个小节"
```

---

## 完成判定(全部满足)

- [ ] `docs/使用手册.md` 顶部版本为 `1.1`,日期 `2026-06-18`
- [ ] 文档版含 10 个二级标题
- [ ] `portal/src/views/Manual.vue` 徽章显示 `v1.1`
- [ ] Vue `chapters` 数组含 10 项
- [ ] Vue 含 `activeChapter === 10` 切换
- [ ] 目录含第十章锚点
- [ ] v1.0 现有 9 章首小节标题未变(逐字保留)
- [ ] 文档版与网页版逐字一致(可人工抽样对比)
- [ ] `vue-tsc --noEmit` 无错误
- [ ] 无新组件、新样式、新路由、新依赖
